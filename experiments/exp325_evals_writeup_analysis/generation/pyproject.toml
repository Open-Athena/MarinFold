[project]
name = "marinfold-writeup-generation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["modal>=1.0", "pyarrow>=23", "pandas>=2.2", "numpy>=2.2", "scikit-learn>=1.7"]

[tool.uv]
package = false
