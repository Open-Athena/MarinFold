[project]
name = "exp308-evals-contact-novelty-search"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = ["numpy", "pandas", "pyarrow", "matplotlib", "scikit-learn", "fsspec", "s3fs"]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.uv]
package = false
