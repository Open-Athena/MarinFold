# Heavy / re-derivable artifacts — never committed (AGENTS rule #2).
.venv/
__pycache__/
*.pyc

# Scratch: downloaded parquets, per-protein score matrices, test plots.
_scratch/

# Derived GT universe (7.6 MB), checkpoint-independent — lives on the HF bucket,
# not git: hf://buckets/open-athena/MarinFold/data/contacts-v1-model-eval-exp89/gt_universe.jsonl
data/gt_universe.jsonl
