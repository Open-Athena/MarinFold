[project]
name = "exp154-models-contacts-v1-parameter-scaling"
version = "0.1.0"
description = "Aggregate contacts-v1 scaling sweep results from W&B for issue #154."
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
    "wandb>=0.19",
]

[tool.uv]
package = false
