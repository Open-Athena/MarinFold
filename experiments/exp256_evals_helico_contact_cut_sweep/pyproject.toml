[project]
name = "exp256-evals-helico-contact-cut-sweep"
version = "0.1.0"
description = "How many MarinFold contacts should Helico be given? The lDDT-versus-cut curve on eval-val — issue #256."
requires-python = ">=3.11"
dependencies = ["numpy", "pandas>=2", "matplotlib>=3.8"]

[dependency-groups]
dev = []

[tool.uv]
package = false
